Drop the background track for /complications here as:

    pal-pal.mp3

Nothing else needs changing — the page picks it up automatically.
If the file is absent the page still works; the music control just hides itself.
